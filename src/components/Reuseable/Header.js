import React from 'react';
import { Layout } from 'antd';

const { Header } = Layout;

const MyHeader = () => {
    return (
        <Header className="header">
            Hello World!
        </Header>
    )
}
export default MyHeader;